# === Django Core ===
Django>=6.0.2                   # CVE-2025-64459 (CVSS 9.1) — SQL injection. Pinned to patched.
djangorestframework>=3.15.2     # CVE-2024-21520 — XSS in break_long_headers. Pinned to patched.
django-filter
mcp>=1.0                        # Official Model Context Protocol Python SDK
django-extensions>=3.2
django-celery-beat
django-cors-headers

# === Async / Channels ===
channels>=4.0                   # WebSockets (Real-time logs)
channels-redis
daphne>=4.0                     # ASGI Server for Channels
asgiref

# === Task Queue ===
celery>=5.3
redis>=5.0                      # Broker & Cache
django-celery-results>=2.5

# === Database ===
psycopg2-binary>=2.9.9
pgvector>=0.3.2

# === AI / ML ===
litellm>=1.83.0                 # Pinned past supply chain incident (1.82.7-1.82.8 compromised)
piper-tts
pydantic

# === Utilities ===
psutil>=5.9                     # System/Process management
requests>=2.31                  # HTTP checks
colorlog>=6.7
Markdown>=3.10
html2text
aiofiles
watchfiles>=1.1.1
tenacity
packaging>=23.0                 # NeuralModifier semver + version_spec resolution
