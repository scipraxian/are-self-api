# Placeholder — the actual config is generated by entrypoint.sh
# based on whether TLS certificates are present in nginx/certs/.
#
# This file exists because docker-compose mounts it into the
# templates directory. The entrypoint overwrites the active config
# at /etc/nginx/conf.d/default.conf before NGINX starts.
