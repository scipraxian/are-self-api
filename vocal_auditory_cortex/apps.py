"""Django app config for vocal_auditory_cortex."""

from django.apps import AppConfig


class VocalAuditoryCortexConfig(AppConfig):
    """Configuration for speech-to-text and text-to-speech."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'vocal_auditory_cortex'
    verbose_name = 'Vocal–Auditory Cortex'
